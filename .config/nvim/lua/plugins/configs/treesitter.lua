local options = {
ensure_installed = { "lua", "ruby", "python", "html", "javascript", "json", "typescript", "go" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
